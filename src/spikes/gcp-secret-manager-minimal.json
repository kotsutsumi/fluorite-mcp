{
  "id": "gcp-secret-manager-minimal",
  "name": "GCP Secret Manager 最小",
  "version": "1.0.0",
  "stack": ["gcp", "secrets"],
  "tags": ["secrets"],
  "description": "GCP Secret Manager から最新バージョンを取得。",
  "params": [ { "name": "app_name", "default": "gcp-secrets" }, { "name": "name", "default": "projects/myproj/secrets/demo/versions/latest" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"scripts\": { \"start\": \"node get.js\" }, \"dependencies\": { \"@google-cloud/secret-manager\": \"latest\" } }\n" },
    { "path": "{{app_name}}/get.js", "template": "const { SecretManagerServiceClient } = require('@google-cloud/secret-manager');\nconst sm = new SecretManagerServiceClient();\n(async () => { const [v] = await sm.accessSecretVersion({ name: '{{name}}' }); console.log('bytes', v.payload?.data?.length||0); })();\n" }
  ],
  "patches": []
}

