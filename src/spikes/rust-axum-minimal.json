{
  "id": "rust-axum-minimal",
  "name": "Rust Axum 最小",
  "version": "1.0.0",
  "stack": ["rust", "axum"],
  "tags": ["web"],
  "description": "axum で最小 HTTP サーバー",
  "params": [ { "name": "app_name", "default": "axum-min" } ],
  "files": [
    { "path": "{{app_name}}/Cargo.toml", "template": "[package]\nname=\"{{app_name}}\"\nversion=\"0.1.0\"\nedition=\"2021\"\n\n[dependencies]\naxum=\"0.7\"\ntokio= { version=\"1\", features=[\"rt-multi-thread\", \"macros\"] }\n" },
    { "path": "{{app_name}}/src/main.rs", "template": "use axum::{routing::get, Router};\n#[tokio::main]\nasync fn main(){ let app = Router::new().route(\"/\", get(|| async { \"Hello Axum\" })); axum::Server::bind(&\"0.0.0.0:3000\".parse().unwrap()).serve(app.into_make_service()).await.unwrap(); }\n" }
  ]
}

