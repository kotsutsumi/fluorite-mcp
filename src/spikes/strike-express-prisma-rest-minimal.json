{
  "id": "strike-express-prisma-rest-minimal",
  "name": "Express + Prisma REST 最小",
  "version": "1.0.0",
  "stack": ["express", "prisma"],
  "tags": ["rest", "db"],
  "description": "ExpressでPrismaを使う最小REST",
  "params": [],
  "files": [
    { "path": "src/server.ts", "template": "import express from 'express';\nconst app = express();\napp.get('/health', (_req, res)=> res.json({ ok: true }));\napp.listen(3000);\n" },
    { "path": "prisma/schema.prisma", "template": "datasource db { provider = \"sqlite\" url = \"file:dev.db\" }\ngenerator client { provider = \"prisma-client-js\" }\nmodel User { id Int @id @default(autoincrement()) email String @unique }\n" }
  ]
}

