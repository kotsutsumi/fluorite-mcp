{
  "id": "kubernetes-secret-minimal",
  "name": "Kubernetes Secret 最小",
  "version": "1.0.0",
  "stack": ["kubernetes", "yaml"],
  "tags": ["k8s"],
  "description": "Opaque Secret の最小",
  "params": [ ],
  "files": [
    { "path": "k8s/secret.yaml", "template": "apiVersion: v1\nkind: Secret\nmetadata: { name: creds }\ntype: Opaque\nstringData: { PASSWORD: pass }\n" }
  ]
}

