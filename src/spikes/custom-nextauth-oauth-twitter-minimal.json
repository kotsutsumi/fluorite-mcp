{
  "id": "custom-nextauth-oauth-twitter-minimal",
  "name": "NextAuth OAuth Twitter 最小",
  "version": "1.0.0",
  "stack": ["next-auth", "nextjs"],
  "tags": ["auth"],
  "description": "Twitter OAuthプロバイダの最小スタブ",
  "files": [
    { "path": "app/api/auth/[...nextauth]/route.ts", "template": "export const GET = async ()=> new Response('ok'); export const POST = GET;\n" }
  ]
}

