{
  "id": "openai-tool-choice-required-minimal",
  "name": "OpenAI Tool Choice Required Minimal (Node)",
  "version": "1.0.0",
  "stack": ["node", "openai"],
  "tags": ["llm", "tools"],
  "description": "Force tool usage with tool_choice='required'.",
  "params": [{ "name": "app_name", "default": "openai-tool-required" }, { "name": "openai_model", "default": "gpt-4o-mini" }],
  "files": [
    { "path": "{{app_name}}/index.mjs", "template": "import OpenAI from 'openai';\nconst client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });\nconst tools = [{ type: 'function', function: { name: 'get_time', description: 'Get current time', parameters: { type: 'object', properties: {} } } }];\nconst res = await client.chat.completions.create({ model: '{{openai_model}}', tool_choice: 'required', messages: [{ role: 'user', content: 'What time is it? Use the tool.' }], tools });\nconsole.log(JSON.stringify(res, null, 2));\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"openai\": \"latest\" } }\n" },
    { "path": "{{app_name}}/.env.example", "template": "OPENAI_API_KEY=sk-...\n" } 
  ],
  "patches": []
}

