{
  "id": "apollo-server-cache-control-minimal",
  "name": "Apollo Server cache control 最小",
  "version": "1.0.0",
  "stack": ["apollo", "graphql"],
  "tags": ["server", "cache"],
  "description": "@apollo/cache-control を使った最小例（v4）",
  "params": [ { "name": "app_name", "default": "apollo-cache-control" } ],
  "files": [
    { "path": "{{app_name}}/src/server.ts", "template": "import { ApolloServer } from '@apollo/server';\nimport { startStandaloneServer } from '@apollo/server/standalone';\nconst typeDefs = `type Query { ok: Boolean! @cacheControl(maxAge: 60) }`;\nconst resolvers = { Query: { ok: ()=> true } };\nconst server = new ApolloServer({ typeDefs, resolvers });\nstartStandaloneServer(server, { listen: { port: 4000 } });\n" }
  ]
}

