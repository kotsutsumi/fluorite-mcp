{
  "id": "loki-http-push-minimal",
  "name": "Loki HTTP Push 最小",
  "version": "1.0.0",
  "stack": ["loki"],
  "tags": ["observability"],
  "description": "Loki HTTP push API でログ送信",
  "files": [ { "path": "run.js", "template": "console.log('POST /loki/api/v1/push with streams [{stream, values}]');\n" } ]
}

