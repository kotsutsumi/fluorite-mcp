{
  "id": "vercel-cron-minimal",
  "name": "Vercel Cron Minimal",
  "version": "1.0.0",
  "stack": ["vercel", "cron"],
  "tags": ["serverless", "schedule"],
  "description": "Vercel の cron で周期実行するエンドポイント。",
  "params": [ { "name": "app_name", "default": "vercel-cron" } ],
  "files": [
    { "path": "{{app_name}}/api/cron.ts", "template": "export const config = { runtime: 'edge' };\nexport default async function handler() { return new Response(JSON.stringify({ ok: true, at: Date.now() }), { headers: { 'content-type': 'application/json' } }); }\n" },
    { "path": "{{app_name}}/vercel.json", "template": "{ \"crons\": [ { \"path\": \"/api/cron\", \"schedule\": \"0 * * * *\" } ] }\n" }
  ],
  "patches": []
}

