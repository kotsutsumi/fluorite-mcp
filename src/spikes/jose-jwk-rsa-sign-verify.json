{
  "id": "jose-jwk-rsa-sign-verify",
  "name": "jose JWK RSA 署名/検証",
  "version": "1.0.0",
  "stack": ["node", "jose"],
  "tags": ["security", "auth"],
  "description": "RSA キー（JWK）で JWT を署名/検証する最小例。",
  "params": [ { "name": "app_name", "default": "jose-jwk-rsa" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"scripts\": { \"start\": \"node index.mjs\" }, \"dependencies\": { \"jose\": \"latest\" } }\n" },
    { "path": "{{app_name}}/index.mjs", "template": "import { generateKeyPair, exportJWK, SignJWT, jwtVerify } from 'jose';\nconst { privateKey, publicKey } = await generateKeyPair('RS256');\nconst jwk = await exportJWK(privateKey);\nconst token = await new SignJWT({ sub: 'user1' }).setProtectedHeader({ alg: 'RS256' }).sign(privateKey);\nconst { payload } = await jwtVerify(token, publicKey);\nconsole.log('sub', payload.sub, 'alg', 'RS256');\n" }
  ],
  "patches": []
}

