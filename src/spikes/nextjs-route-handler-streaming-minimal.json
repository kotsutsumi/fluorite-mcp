{
  "id": "nextjs-route-handler-streaming-minimal",
  "name": "Next.js Route Handler Streaming 最小",
  "version": "1.0.0",
  "stack": ["nextjs"],
  "tags": ["route", "stream"],
  "description": "ReadableStream を返す最小ストリーミング応答",
  "params": [ { "name": "app_name", "default": "next-stream" } ],
  "files": [
    { "path": "{{app_name}}/app/api/stream/route.ts", "template": "export async function GET(){ const encoder = new TextEncoder(); const stream = new ReadableStream({ start(c){ c.enqueue(encoder.encode('hello')); c.close(); } }); return new Response(stream); }\n" }
  ]
}

