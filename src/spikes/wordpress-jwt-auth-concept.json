{
  "id": "wordpress-jwt-auth-concept",
  "name": "WordPress JWT Auth (概念)",
  "version": "1.0.0",
  "stack": ["wordpress"],
  "tags": ["cms", "auth"],
  "description": "WP JWT 認証プラグインを用いた API 認証の概念",
  "files": [ { "path": "README.md", "template": "Use JWT Auth plugin, obtain token via /jwt-auth/v1/token, pass Authorization: Bearer.\n" } ]
}

