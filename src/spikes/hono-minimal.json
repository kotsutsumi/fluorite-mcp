{
  "id": "hono-minimal",
  "name": "Hono 最小サーバー",
  "version": "1.0.0",
  "stack": ["hono"],
  "tags": ["web"],
  "description": "Hono の最小 API サーバー",
  "files": [
    { "path": "server.ts", "template": "import { Hono } from 'hono';\nconst app = new Hono();\napp.get('/', c=> c.text('ok'));\nexport default app;\n" }
  ]
}

