{
  "id": "hashicorp-vault-read-secret-minimal",
  "name": "Vault シークレット取得 最小",
  "version": "1.0.0",
  "stack": ["vault", "node"],
  "tags": ["security"],
  "description": "node-vault を使ったシークレット取得",
  "params": [ { "name": "path", "default": "secret/data/app" } ],
  "files": [
    { "path": "vault/read.mjs", "template": "import vault from 'node-vault';\nconst client = vault({ endpoint: process.env.VAULT_ADDR || 'http://127.0.0.1:8200', token: process.env.VAULT_TOKEN });\nconsole.log(await client.read(process.env.VAULT_PATH || '{{path}}'));\n" }
  ]
}

