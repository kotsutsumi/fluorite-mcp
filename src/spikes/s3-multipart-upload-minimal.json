{
  "id": "s3-multipart-upload-minimal",
  "name": "AWS S3 マルチパートアップロード 最小",
  "version": "1.0.0",
  "stack": ["node", "aws"],
  "tags": ["storage"],
  "description": "S3 マルチパートアップロードの雛形 (v3 SDK)",
  "params": [ { "name": "app_name", "default": "s3-multipart" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"type\": \"module\", \"scripts\": { \"start\": \"node upload.js\" }, \"dependencies\": { \"@aws-sdk/client-s3\": \"^3\", \"@aws-sdk/lib-storage\": \"^3\" } }\n" },
    { "path": "{{app_name}}/upload.js", "template": "import { S3Client } from '@aws-sdk/client-s3';\nimport { Upload } from '@aws-sdk/lib-storage';\nconst s3 = new S3Client({});\nconsole.log('new Upload({ client: s3, params:{ Bucket, Key, Body }})');\n" }
  ]
}

