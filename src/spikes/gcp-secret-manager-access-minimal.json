{
  "id": "gcp-secret-manager-access-minimal",
  "name": "GCP Secret Manager 取得 最小",
  "version": "1.0.0",
  "stack": ["gcp", "secret-manager", "node"],
  "tags": ["security"],
  "description": "@google-cloud/secret-manager を用いた秘密取得最小例",
  "params": [ { "name": "secretName", "default": "projects/123/secrets/my/versions/latest" } ],
  "files": [
    { "path": "gcp/secret.mjs", "template": "import { SecretManagerServiceClient } from '@google-cloud/secret-manager';\nconst client = new SecretManagerServiceClient();\nconst [v] = await client.accessSecretVersion({ name: process.env.SECRET_NAME || '{{secretName}}' });\nconsole.log(v.payload?.data?.toString());\n" }
  ]
}

