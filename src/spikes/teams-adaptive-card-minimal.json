{
  "id": "teams-adaptive-card-minimal",
  "name": "Teams Adaptive Card 送信 最小",
  "version": "1.0.0",
  "stack": ["node", "teams"],
  "tags": ["chat"],
  "description": "Incoming Webhook へ Adaptive Card",
  "params": [ { "name": "app_name", "default": "teams-card" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{\"name\":\"{{app_name}}\",\"private\":true,\"type\":\"module\",\"scripts\":{\"start\":\"node send.js\"},\"dependencies\":{\"node-fetch\":\"^3\"}}\n" },
    { "path": "{{app_name}}/send.js", "template": "import fetch from 'node-fetch';\nawait fetch(process.env.TEAMS_WEBHOOK_URL, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ type:'message', attachments:[{ contentType:'application/vnd.microsoft.card.adaptive', content: { type:'AdaptiveCard', version:'1.4', body:[{ type:'TextBlock', text:'Hello' }] } }] }) });\nconsole.log('sent');\n" }
  ]
}

