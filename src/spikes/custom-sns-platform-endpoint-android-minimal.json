{
  "id": "custom-sns-platform-endpoint-android-minimal",
  "name": "SNS Platform Endpoint ANDROID 最小",
  "version": "1.0.0",
  "stack": ["sns"],
  "tags": ["push", "aws"],
  "description": "Androidエンドポイント作成（概念）",
  "files": [
    { "path": "src/aws/sns.android.ts", "template": "export async function createEndpoint(token:string){ return { endpoint: 'arn:aws:sns:android:...', token }; }\n" }
  ]
}

