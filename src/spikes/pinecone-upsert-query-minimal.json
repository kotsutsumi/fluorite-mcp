{
  "id": "pinecone-upsert-query-minimal",
  "name": "Pinecone Upsert + Query (Node)",
  "version": "1.0.0",
  "stack": ["node", "pinecone"],
  "tags": ["vector", "database"],
  "description": "Upsert a vector and run a query with Pinecone SDK.",
  "params": [
    { "name": "app_name", "default": "pinecone-minimal-app" },
    { "name": "pinecone_index", "default": "test-index" }
  ],
  "files": [
    { "path": "{{app_name}}/index.mjs", "template": "import { Pinecone } from '@pinecone-database/pinecone';\nconst pc = new Pinecone({ apiKey: process.env.PINECONE_API_KEY });\nconst index = pc.Index(process.env.PINECONE_INDEX || '{{pinecone_index}}');\nawait index.upsert([{ id: 'a', values: [0.1,0.2,0.3], metadata: { text: 'hello world' } }]);\nconst res = await index.query({ topK: 3, vector: [0.1,0.2,0.3], includeMetadata: true });\nconsole.log(res.matches?.map(m => m.metadata?.text));\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"@pinecone-database/pinecone\": \"latest\" } }\n" },
    { "path": "{{app_name}}/.env.example", "template": "PINECONE_API_KEY=...\nPINECONE_INDEX={{pinecone_index}}\n" }
  ],
  "patches": []
}

