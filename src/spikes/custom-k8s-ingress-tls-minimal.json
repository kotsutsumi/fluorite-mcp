{
  "id": "custom-k8s-ingress-tls-minimal",
  "name": "K8s Ingress TLS 最小",
  "version": "1.0.0",
  "stack": ["kubernetes"],
  "tags": ["k8s"],
  "description": "TLS設定付きIngress最小",
  "files": [
    { "path": "k8s/ingress-tls.yml", "template": "apiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata: { name: app }\nspec: { tls: [{ hosts: ['example.com'], secretName: 'tls-secret' }], rules: [] }\n" }
  ]
}

