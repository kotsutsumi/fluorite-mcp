{
  "id": "gitlab-merge-request-create-minimal",
  "name": "GitLab Merge Request 作成 最小",
  "version": "1.0.0",
  "stack": ["node", "gitlab"],
  "tags": ["gitlab"],
  "description": "gitlab SDK で MR 作成",
  "params": [ { "name": "app_name", "default": "gitlab-mr" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{\\n  \"name\": \"{{app_name}}\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": { \"start\": \"node run.js\" },\n  \"dependencies\": { \"gitlab\": \"^16\" }\n}\n" },
    { "path": "{{app_name}}/run.js", "template": "import { Gitlab } from 'gitlab';\nconst api = new Gitlab({ host: process.env.GITLAB_HOST||'https://gitlab.com', token: process.env.GITLAB_TOKEN });\nconsole.log('Use api.MergeRequests.create(projectId, source, target, title)');\n" }
  ]
}

