{
  "id": "multer-express-upload-minimal",
  "name": "Multer Express アップロード 最小",
  "version": "1.0.0",
  "stack": ["express", "multer"],
  "tags": ["file"],
  "description": "multer を使った単一ファイルアップロード",
  "params": [ { "name": "app_name", "default": "upload-app" } ],
  "files": [
    { "path": "{{app_name}}/server.js", "template": "const express = require('express');\nconst multer = require('multer');\nconst upload = multer({ dest: 'uploads/' });\nconst app = express();\napp.post('/upload', upload.single('file'), (req, res) => res.json({ file: req.file?.originalname }));\napp.listen(3000);\n" }
  ]
}

