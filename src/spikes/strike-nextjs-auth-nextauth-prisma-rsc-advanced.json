{
  "id": "strike-nextjs-auth-nextauth-prisma-rsc-advanced",
  "name": "Next.js + NextAuth + Prisma (RSC/Advanced)",
  "version": "1.0.0",
  "stack": ["nextjs","authjs","prisma","rsc"],
  "tags": ["auth","db","advanced","strike"],
  "description": "NextAuthのApp Routerルート、Prismaスキーマ、RSC対応の基本土台。",
  "files": [
    { "path": "app/api/auth/[...nextauth]/route.ts", "template": "import NextAuth from 'next-auth';\nimport { authOptions } from '@/src/auth/options';\nconst handler = NextAuth(authOptions as any);\nexport { handler as GET, handler as POST };\n" },
    { "path": "src/auth/options.ts", "template": "// NextAuth options (PrismaAdapter想定)\n// 実際の実装では @auth/prisma-adapter を導入し、PrismaClientを接続してください。\nexport const authOptions = {\n  providers: [],\n  session: { strategy: 'jwt' },\n};\n" },
    { "path": "prisma/schema.prisma", "template": "datasource db { provider = \"postgresql\" url = env(\"DATABASE_URL\") }\ngenerator client { provider = \"prisma-client-js\" }\nmodel User { id String @id @default(cuid()) email String? @@map(\"users\") }\n" }
  ]
}

