{
  "id": "custom-kysely-json-operators-minimal",
  "name": "Kysely JSON 演算子 最小",
  "version": "1.0.0",
  "stack": ["kysely"],
  "tags": ["db"],
  "description": "JSON演算子（->,->>）の概念",
  "files": [
    { "path": "src/db/kysely.json.ts", "template": "export async function getPath(){ return { value: null }; }\n" }
  ]
}

