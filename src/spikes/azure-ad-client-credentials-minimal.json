{
  "id": "azure-ad-client-credentials-minimal",
  "name": "Azure AD Client Credentials 最小",
  "version": "1.0.0",
  "stack": ["node", "azure"],
  "tags": ["auth"],
  "description": "@azure/identity で機密クライアントのトークン取得",
  "params": [ { "name": "app_name", "default": "azure-ad-cc" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"type\": \"module\", \"scripts\": { \"start\": \"node token.js\" }, \"dependencies\": { \"@azure/identity\": \"^4\" } }\n" },
    { "path": "{{app_name}}/token.js", "template": "import { ClientSecretCredential } from '@azure/identity';\nconst cred = new ClientSecretCredential(process.env.AZ_TENANT, process.env.AZ_CLIENT, process.env.AZ_SECRET);\nconsole.log('Use cred.getToken(scope)');\n" }
  ]
}

