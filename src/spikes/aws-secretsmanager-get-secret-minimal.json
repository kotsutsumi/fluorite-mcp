{
  "id": "aws-secretsmanager-get-secret-minimal",
  "name": "AWS Secrets Manager 取得 最小",
  "version": "1.0.0",
  "stack": ["aws", "secretsmanager", "node"],
  "tags": ["security"],
  "description": "Secrets Manager のシークレット取得最小例",
  "params": [ { "name": "secretId", "default": "my/secret" } ],
  "files": [
    { "path": "aws/secret.mjs", "template": "import { SecretsManagerClient, GetSecretValueCommand } from '@aws-sdk/client-secrets-manager';\nconst client = new SecretsManagerClient({ region: process.env.AWS_REGION || 'us-east-1' });\nconst out = await client.send(new GetSecretValueCommand({ SecretId: process.env.SECRET_ID || '{{secretId}}' }));\nconsole.log(out.SecretString);\n" }
  ]
}

