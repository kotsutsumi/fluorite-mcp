{
  "id": "custom-vercel-cron-edge-job-route-minimal",
  "name": "Vercel Cron Edge Job ルート 最小",
  "version": "1.0.0",
  "stack": ["vercel", "nextjs"],
  "tags": ["edge", "cron"],
  "description": "Edge RuntimeでのCronジョブルート（概念）",
  "files": [
    { "path": "app/api/cron/edge/route.ts", "template": "import { NextResponse } from 'next/server'; export async function GET(){ return NextResponse.json({ ok: true }); }\n" }
  ]
}

