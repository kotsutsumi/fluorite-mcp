{
  "id": "apns-http2-send-concept",
  "name": "APNs HTTP/2 送信 (概念)",
  "version": "1.0.0",
  "stack": ["apns"],
  "tags": ["push"],
  "description": "Apple Push のHTTP/2送信概念",
  "files": [ { "path": "README.md", "template": "POST to api.push.apple.com with JWT auth (p8 key).\n" } ]
}

