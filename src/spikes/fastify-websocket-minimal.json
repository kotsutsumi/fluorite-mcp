{
  "id": "fastify-websocket-minimal",
  "name": "Fastify WebSocket 最小",
  "version": "1.0.0",
  "stack": ["fastify", "ws"],
  "tags": ["web", "realtime"],
  "description": "@fastify/websocket を用いたWS",
  "files": [ { "path": "server.js", "template": "const Fastify=require('fastify'); const ws=require('@fastify/websocket'); const app=Fastify(); app.register(ws); app.get('/ws',{ websocket:true }, (conn)=>{ conn.socket.send('hi'); }); app.listen({ port:3000 });\n" } ]
}

