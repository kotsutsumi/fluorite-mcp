{
  "id": "nextauth-azuread-provider",
  "name": "NextAuth Azure AD Provider",
  "version": "1.0.0",
  "stack": ["nextjs", "nextauth"],
  "tags": ["auth", "oauth"],
  "description": "Configure NextAuth with Azure AD (Entra ID) provider.",
  "params": [{ "name": "app_name", "default": "next-app" }],
  "files": [
    { "path": "{{app_name}}/app/api/auth/[...nextauth]/route.ts", "template": "import NextAuth from 'next-auth';\nimport AzureAD from 'next-auth/providers/azure-ad';\nconst handler = NextAuth({ providers: [AzureAD] });\nexport { handler as GET, handler as POST };\n" },
    { "path": "{{app_name}}/.env.example", "template": "AUTH_AZURE_AD_ID=\nAUTH_AZURE_AD_SECRET=\nNEXTAUTH_URL=http://localhost:3000\nNEXTAUTH_SECRET=changeme\n" }
  ],
  "patches": []
}

