{
  "id": "custom-prisma-transaction-retry-minimal",
  "name": "Prisma トランザクション リトライ 最小",
  "version": "1.0.0",
  "stack": ["prisma"],
  "tags": ["db", "transaction"],
  "description": "トランザクションの簡易リトライユーティリティ",
  "files": [
    { "path": "src/prisma.retry.ts", "template": "export async function withRetry<T>(fn:()=>Promise<T>, n=3){ let e:any; for(let i=0;i<n;i++){ try{ return await fn(); }catch(err){ e=err; } } throw e; }\n" }
  ]
}

