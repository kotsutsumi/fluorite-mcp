{
  "id": "nextjs-dynamic-route-segment-minimal",
  "name": "Next.js Dynamic Route Segment 最小",
  "version": "1.0.0",
  "stack": ["nextjs"],
  "tags": ["frontend"],
  "description": "[slug] の基本",
  "files": [ { "path": "app/[slug]/page.tsx", "template": "export default function Page({ params }:{ params:{ slug:string }}){ return <div>{params.slug}</div> }\n" } ]
}

