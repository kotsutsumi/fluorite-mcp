{
  "id": "next-shadcn-sheet",
  "name": "Next.js shadcn/ui Sheet",
  "version": "1.0.0",
  "stack": ["nextjs", "shadcn"],
  "tags": ["ui", "sheet"],
  "description": "Side sheet panel using utility classes.",
  "params": [{ "name": "app_name", "default": "next-app" }],
  "files": [
    { "path": "{{app_name}}/components/ui/sheet.tsx", "template": "import * as React from 'react';\nexport function Sheet(){ const [o,setO]=React.useState(false); return (<div><button className='border px-2 py-1' onClick={()=>setO(true)}>Open</button>{o&&(<div className='fixed inset-0 bg-black/30' onClick={()=>setO(false)}><div className='fixed top-0 right-0 bottom-0 w-80 bg-white p-4 shadow-2xl' onClick={e=>e.stopPropagation()}><div className='text-right'><button className='border px-2 py-1' onClick={()=>setO(false)}>Close</button></div><div>Sheet content</div></div></div>)}</div>); }\n" },
    { "path": "{{app_name}}/app/page.tsx", "template": "import { Sheet } from '../components/ui/sheet';\nexport default function Page(){ return <main className='p-6'><Sheet/></main>; }\n" }
  ],
  "patches": []
}

