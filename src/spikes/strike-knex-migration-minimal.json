{
  "id": "strike-knex-migration-minimal",
  "name": "Knex Migration Minimal",
  "stack": [
    "node",
    "knex"
  ],
  "tags": [
    "db"
  ],
  "files": [
    {
      "path": "migrations/001_init.js",
      "template": "export async function up(knex){ await knex.schema.createTable('users', t=>{ t.increments('id'); t.string('email'); }); }\nexport async function down(knex){ await knex.schema.dropTable('users'); }\n"
    }
  ],
  "patches": []
}
