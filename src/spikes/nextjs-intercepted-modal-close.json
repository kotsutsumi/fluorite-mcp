{
  "id": "nextjs-intercepted-modal-close",
  "name": "Next.js Intercepted Modal Close Pattern",
  "version": "1.0.0",
  "stack": ["nextjs"],
  "tags": ["app-router", "intercept"],
  "description": "Modal overlay with close navigation to underlying route.",
  "params": [{ "name": "app_name", "default": "next-app" }],
  "files": [
    { "path": "{{app_name}}/app/(modal)/layout.tsx", "template": "export default function ModalLayout({ children }: { children: React.ReactNode }){ return <>{children}</>; }\n" },
    { "path": "{{app_name}}/app/(modal)/gallery/(.)photo/page.tsx", "template": "import Link from 'next/link'; export default function Photo(){ return (<div style={{ position:'fixed', inset:0, background:'rgba(0,0,0,0.5)' }}><div style={{ margin:'10% auto', background:'#fff', padding:20, width:300 }}><p>Photo modal</p><Link href='/gallery'>Close</Link></div></div>); }\n" },
    { "path": "{{app_name}}/app/gallery/page.tsx", "template": "import Link from 'next/link'; export default function Gallery(){ return (<main>Gallery <Link href='/gallery/photo'>Open</Link></main>); }\n" }
  ],
  "patches": []
}

