{
  "id": "ghost-webhook-create-minimal",
  "name": "Ghost Webhook 作成 最小",
  "version": "1.0.0",
  "stack": ["ghost"],
  "tags": ["cms", "webhook"],
  "description": "Ghost Admin API で webhook 作成",
  "files": [ { "path": "README.md", "template": "POST /ghost/api/admin/webhooks/ with target URL and event.\n" } ]
}

