{
  "id": "prisma-sqlite-memory-minimal",
  "name": "Prisma + SQLite メモリ 最小",
  "version": "1.0.0",
  "stack": ["prisma", "sqlite", "node"],
  "tags": ["db"],
  "description": "SQLite :memory: 接続の最小スキーマ。",
  "params": [ { "name": "app_name", "default": "prisma-sqlite-memory" } ],
  "files": [
    { "path": "{{app_name}}/prisma/schema.prisma", "template": "generator client { provider = \"prisma-client-js\" }\ndatasource db { provider = \"sqlite\" url = \"file::memory:?cache=shared\" }\nmodel Item { id Int @id @default(autoincrement()) name String }\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"scripts\": { \"postinstall\": \"prisma generate\" }, \"dependencies\": { \"@prisma/client\": \"latest\" }, \"devDependencies\": { \"prisma\": \"latest\" } }\n" }
  ],
  "patches": []
}

