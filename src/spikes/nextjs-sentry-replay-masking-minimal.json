{
  "id": "nextjs-sentry-replay-masking-minimal",
  "name": "Next.js Sentry Replay Masking 最小",
  "version": "1.0.0",
  "stack": ["nextjs", "sentry"],
  "tags": ["observability", "replay", "privacy"],
  "description": "Sentry Replay でPIIマスキング設定の最小例",
  "params": [ { "name": "app_name", "default": "next-sentry-replay-mask" } ],
  "files": [
    { "path": "{{app_name}}/sentry.client.config.ts", "template": "import * as Sentry from '@sentry/nextjs';\nimport { replayIntegration } from '@sentry/replay';\nSentry.init({ dsn: process.env.SENTRY_DSN || '', integrations: [replayIntegration({ block: ['input[type=\\"password\\"]','[data-sensitive]'] })], replaysSessionSampleRate: 1.0, replaysOnErrorSampleRate: 1.0 });\n" }
  ]
}

