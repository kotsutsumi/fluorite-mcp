{
  "id": "strike-zod-refine-password-policy",
  "name": "Zod パスワードポリシー refine",
  "version": "1.0.0",
  "stack": ["zod"],
  "tags": ["validation","security","strike"],
  "description": "Zod refineで強固なパスワードポリシーを実装する雛形。",
  "files": [
    { "path": "src/validation/password.ts", "template": "import { z } from 'zod';\nexport const Password = z.string().min(12).refine(v=>/[A-Z]/.test(v) && /[0-9]/.test(v) && /[^A-Za-z0-9]/.test(v), 'weak');\n" }
  ]
}

