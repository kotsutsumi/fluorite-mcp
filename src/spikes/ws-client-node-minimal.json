{
  "id": "ws-client-node-minimal",
  "name": "ws WebSocket クライアント 最小",
  "version": "1.0.0",
  "stack": ["ws", "node"],
  "tags": ["realtime"],
  "description": "ws を用いたクライアント接続",
  "params": [ ],
  "files": [
    { "path": "ws/client.js", "template": "const WebSocket = require('ws');\nconst ws = new WebSocket('ws://localhost:8080');\nws.on('open', ()=> ws.send('hello'));\nws.on('message', m => console.log(String(m)));\n" }
  ]
}

