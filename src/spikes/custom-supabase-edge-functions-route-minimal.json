{
  "id": "custom-supabase-edge-functions-route-minimal",
  "name": "Supabase Edge Functions ルート 最小",
  "version": "1.0.0",
  "stack": ["supabase"],
  "tags": ["edge"],
  "description": "Edge Functions呼び出しルート（スタブ）",
  "files": [
    { "path": "app/api/supabase/edge/route.ts", "template": "import { NextResponse } from 'next/server'; export async function GET(){ return NextResponse.json({ ok: true }); }\n" }
  ]
}

