{
  "id": "custom-vault-read-secret-route-minimal",
  "name": "Vault シークレット取得 ルート 最小",
  "version": "1.0.0",
  "stack": ["vault", "nextjs"],
  "tags": ["secrets"],
  "description": "Vaultからシークレットを取得するNext.jsルート（スタブ）",
  "files": [
    { "path": "app/api/secrets/get/route.ts", "template": "import { NextResponse } from 'next/server';\nexport async function GET(){ return NextResponse.json({ value: 'secret' }); }\n" }
  ]
}

