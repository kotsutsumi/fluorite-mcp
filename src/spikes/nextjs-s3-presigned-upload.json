{
  "id": "nextjs-s3-presigned-upload",
  "name": "Next.js S3 Presigned Upload",
  "version": "1.0.0",
  "stack": ["nextjs", "aws-s3"],
  "tags": ["storage", "upload"],
  "description": "Generate presigned URL in Route Handler for S3 upload.",
  "params": [{ "name": "app_name", "default": "next-app" }],
  "files": [
    { "path": "{{app_name}}/app/api/upload-url/route.ts", "template": "import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';\nimport { getSignedUrl } from '@aws-sdk/s3-request-presigner';\nexport async function GET(){\n  const s3 = new S3Client({});\n  const url = await getSignedUrl(s3, new PutObjectCommand({ Bucket: process.env.S3_BUCKET!, Key: 'upload.bin' }), { expiresIn: 300 });\n  return Response.json({ url });\n}\n" },
    { "path": "{{app_name}}/.env.example", "template": "AWS_ACCESS_KEY_ID=\nAWS_SECRET_ACCESS_KEY=\nAWS_REGION=\nS3_BUCKET=\n" }
  ],
  "patches": []
}

