{
  "id": "strike-zustand-store-minimal",
  "name": "Zustand Store Minimal",
  "stack": [
    "react",
    "zustand"
  ],
  "tags": [
    "state"
  ],
  "files": [
    {
      "path": "src/store.ts",
      "template": "import { create } from 'zustand';\nexport const useStore = create<{ count: number; inc(): void }>((set)=>({ count:0, inc:()=> set(s=>({ count: s.count+1 })) }));\n"
    }
  ],
  "patches": []
}
