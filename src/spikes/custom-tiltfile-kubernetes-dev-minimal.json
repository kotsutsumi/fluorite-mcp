{
  "id": "custom-tiltfile-kubernetes-dev-minimal",
  "name": "Tiltfile K8s Dev 最小",
  "version": "1.0.0",
  "stack": ["tilt"],
  "tags": ["dev"],
  "description": "Tiltの最小",
  "files": [
    { "path": "Tiltfile", "template": "k8s_yaml('k8s/deploy.yml')\n" }
  ]
}

