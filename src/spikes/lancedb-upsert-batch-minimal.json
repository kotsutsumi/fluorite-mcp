{
  "id": "lancedb-upsert-batch-minimal",
  "name": "LanceDB Upsert Batch Minimal (Node)",
  "version": "1.0.0",
  "stack": ["node", "lancedb"],
  "tags": ["upsert"],
  "description": "Upsert multiple rows into a LanceDB table.",
  "params": [{ "name": "app_name", "default": "lancedb-upsert-batch" }],
  "files": [
    { "path": "{{app_name}}/index.mjs", "template": "import { connect } from '@lancedb/lancedb';\nconst db = await connect('./.lancedb');\nconst table = await db.createTable('docs', [{ id: '1', vector: [0.1,0.2,0.3], text: 'hello' }]);\nawait table.upsert([{ id: '2', vector: [0.2,0.1,0.25], text: 'fluorite' }]);\nconst res = await table.toArray();\nconsole.log(res.map(r=>r.id));\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"@lancedb/lancedb\": \"latest\" } }\n" }
  ],
  "patches": []
}

