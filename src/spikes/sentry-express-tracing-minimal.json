{
  "id": "sentry-express-tracing-minimal",
  "name": "Sentry Express Tracing 最小",
  "version": "1.0.0",
  "stack": ["node", "express", "sentry"],
  "tags": ["observability"],
  "description": "@sentry/node の Express Tracing 初期化",
  "params": [ { "name": "app_name", "default": "sentry-express" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"scripts\": { \"start\": \"node server.js\" }, \"dependencies\": { \"express\": \"^4\", \"@sentry/node\": \"^8\", \"@sentry/tracing\": \"^7\" } }\n" },
    { "path": "{{app_name}}/server.js", "template": "const express = require('express');\nconst Sentry = require('@sentry/node');\nSentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 1.0 });\nconst app = express();\napp.get('/', (_req,res)=>res.send('ok'));\napp.listen(3000, ()=>console.log('listening'));\n" }
  ]
}

