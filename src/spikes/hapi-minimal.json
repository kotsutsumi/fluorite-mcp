{
  "id": "hapi-minimal",
  "name": "hapi Minimal API",
  "version": "1.0.0",
  "stack": ["node", "hapi"],
  "tags": ["api"],
  "description": "Minimal hapi server with two routes.",
  "params": [
    { "name": "app_name", "default": "hapi-app" },
    { "name": "port", "default": "3000" }
  ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"scripts\": { \"start\": \"node index.js\" }, \"dependencies\": { \"@hapi/hapi\": \"latest\" } }\n" },
    { "path": "{{app_name}}/index.js", "template": "const Hapi = require('@hapi/hapi');\nasync function start() {\n  const server = Hapi.server({ port: {{port}}, host: '0.0.0.0' });\n  server.route({ method: 'GET', path: '/health', handler: () => ({ status: 'ok' }) });\n  server.route({ method: 'GET', path: '/', handler: () => ({ hello: 'world' }) });\n  await server.start();\n  console.log('Server running on', server.info.uri);\n}\nstart().catch(console.error);\n" }
  ],
  "patches": []
}

