{
  "id": "simple-oauth2-client-credentials-minimal",
  "name": "simple-oauth2 Client Credentials 最小",
  "version": "1.0.0",
  "stack": ["oauth2", "node"],
  "tags": ["auth"],
  "description": "simple-oauth2 でクライアントクレデンシャル取得",
  "params": [ ],
  "files": [
    { "path": "oauth2/token.mjs", "template": "import * as oauth from 'simple-oauth2';\nconst client = new oauth.ClientCredentials({ client: { id: process.env.CLIENT_ID, secret: process.env.CLIENT_SECRET }, auth: { tokenHost: process.env.TOKEN_HOST } });\nconsole.log(await client.getToken({}));\n" }
  ]
}

