{
  "id": "custom-airtable-crud-route-minimal",
  "name": "Airtable CRUD ルート 最小",
  "version": "1.0.0",
  "stack": ["airtable", "nextjs"],
  "tags": ["cms"],
  "description": "Airtable CRUDの最小ルート",
  "files": [
    { "path": "app/api/airtable/route.ts", "template": "import { NextResponse } from 'next/server'; export async function GET(){ return NextResponse.json({ rows: [] }); }\n" }
  ]
}

