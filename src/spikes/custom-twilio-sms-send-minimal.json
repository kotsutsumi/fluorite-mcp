{
  "id": "custom-twilio-sms-send-minimal",
  "name": "Twilio SMS 送信 最小",
  "version": "1.0.0",
  "stack": ["twilio"],
  "tags": ["sms"],
  "description": "TwilioでSMS送信の最小スタブ",
  "files": [
    { "path": "src/sms/twilio.ts", "template": "export async function send(to:string, body:string){ return { to, body }; }\n" }
  ]
}

