{
  "id": "openai-tool-choice-specific-minimal",
  "name": "OpenAI Tool Choice Specific Minimal (Node)",
  "version": "1.0.0",
  "stack": ["node", "openai"],
  "tags": ["llm", "tools"],
  "description": "Force a specific function with tool_choice specifying the function name.",
  "params": [{ "name": "app_name", "default": "openai-tool-specific" }, { "name": "openai_model", "default": "gpt-4o-mini" }],
  "files": [
    { "path": "{{app_name}}/index.mjs", "template": "import OpenAI from 'openai';\nconst client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });\nconst tools = [{ type: 'function', function: { name: 'get_time', description: 'Get current time', parameters: { type: 'object', properties: {} } } }];\nconst res = await client.chat.completions.create({ model: '{{openai_model}}', tool_choice: { type: 'function', function: { name: 'get_time' } }, messages: [{ role: 'user', content: 'What time is it?' }], tools });\nconsole.log(JSON.stringify(res, null, 2));\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"openai\": \"latest\" } }\n" },
    { "path": "{{app_name}}/.env.example", "template": "OPENAI_API_KEY=sk-...\n" } 
  ],
  "patches": []
}

