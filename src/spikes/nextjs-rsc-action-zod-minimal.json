{
  "id": "nextjs-rsc-action-zod-minimal",
  "name": "Next.js RSC Action × Zod 最小",
  "version": "1.0.0",
  "stack": ["nextjs", "zod"],
  "tags": ["rsc", "validation"],
  "description": "サーバーアクションで Zod バリデーションを行う最小例",
  "params": [ { "name": "app_name", "default": "next-rsc-zod" } ],
  "files": [
    { "path": "{{app_name}}/app/actions/save.ts", "template": "'use server';\nimport { z } from 'zod';\nconst Schema = z.object({ email: z.string().email() });\nexport async function save(data: unknown){ const v = Schema.safeParse(data); if(!v.success) return { ok:false }; return { ok:true }; }\n" }
  ]
}

