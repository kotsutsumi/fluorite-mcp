{
  "id": "fastify-ajv-schema-minimal",
  "name": "Fastify AJV スキーマ 最小",
  "version": "1.0.0",
  "stack": ["fastify", "ajv"],
  "tags": ["validation"],
  "description": "AJV スキーマでのバリデーション",
  "params": [ ],
  "files": [
    { "path": "fastify/server.js", "template": "const Fastify = require('fastify');\nconst app = Fastify();\napp.post('/hello', { schema: { body: { type: 'object', properties: { name: { type: 'string' } }, required: ['name'] } } }, (req, reply)=> reply.send({ ok: true }));\napp.listen({ port: 3000 });\n" }
  ]
}

