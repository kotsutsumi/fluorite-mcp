{
  "id": "prometheus-scrape-next-metrics",
  "name": "Prometheus Scrape Config for Next.js /metrics",
  "version": "1.0.0",
  "stack": ["prometheus"],
  "tags": ["metrics", "scrape"],
  "description": "Prometheus config targeting Next.js /api/metrics endpoint.",
  "params": [{ "name": "project_name", "default": "obs" }],
  "files": [
    { "path": "{{project_name}}/prometheus.yml", "template": "global:\n  scrape_interval: 15s\nscrape_configs:\n  - job_name: 'nextjs'\n    metrics_path: /api/metrics\n    static_configs:\n      - targets: ['host.docker.internal:3000']\n" }
  ],
  "patches": []
}

