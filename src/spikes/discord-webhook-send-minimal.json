{
  "id": "discord-webhook-send-minimal",
  "name": "Discord Webhook 送信 最小",
  "version": "1.0.0",
  "stack": ["discord"],
  "tags": ["chat"],
  "description": "Webhook URL へ送信",
  "files": [ { "path": "run.js", "template": "console.log('POST webhook url with { content }');\n" } ]
}

