{
  "id": "docker-compose-mysql",
  "name": "Docker Compose - MySQL",
  "version": "1.0.0",
  "stack": ["docker", "mysql"],
  "tags": ["infra", "docker"],
  "description": "Minimal docker-compose for MySQL 8.",
  "params": [ { "name": "app_name", "default": "mysql-stack" } ],
  "files": [
    { "path": "{{app_name}}/docker-compose.yml", "template": "version: '3.9'\nservices:\n  mysql:\n    image: mysql:8\n    environment:\n      - MYSQL_ROOT_PASSWORD=example\n      - MYSQL_DATABASE=app\n    ports: ['3306:3306']\n    command: ['--default-authentication-plugin=mysql_native_password']\n" }
  ],
  "patches": []
}

