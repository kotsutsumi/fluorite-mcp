{
  "id": "next-shadcn-badge",
  "name": "Next.js shadcn/ui Badge",
  "version": "1.0.0",
  "stack": ["nextjs", "shadcn"],
  "tags": ["ui", "badge"],
  "description": "Small rounded badge component (utility classes).",
  "params": [{ "name": "app_name", "default": "next-app" }],
  "files": [
    { "path": "{{app_name}}/components/ui/badge.tsx", "template": "export function Badge({ children }: { children: React.ReactNode }){ return (<span className='inline-flex items-center rounded-full bg-gray-100 px-2 py-0.5 text-xs font-medium text-gray-800'>{children}</span>); }\n" },
    { "path": "{{app_name}}/app/page.tsx", "template": "import { Badge } from '../components/ui/badge'; export default function Page(){ return <main className='p-6'><Badge>New</Badge></main>; }\n" }
  ],
  "patches": []
}

