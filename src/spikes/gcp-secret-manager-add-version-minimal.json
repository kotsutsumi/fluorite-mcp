{
  "id": "gcp-secret-manager-add-version-minimal",
  "name": "GCP Secret Manager Add Version 最小",
  "version": "1.0.0",
  "stack": ["gcp", "secret-manager"],
  "tags": ["security"],
  "description": "新しいバージョンを追加",
  "params": [ { "name": "secretName", "default": "projects/123/secrets/my" } ],
  "files": [ { "path": "gcp/secret-add.mjs", "template": "import { SecretManagerServiceClient } from '@google-cloud/secret-manager';\nconst c = new SecretManagerServiceClient();\nconsole.log(await c.addSecretVersion({ parent: process.env.SECRET_NAME||'{{secretName}}', payload: { data: Buffer.from('hello') } }));\n" } ]
}

