{
  "id": "drizzle-json-field-minimal",
  "name": "Drizzle JSON フィールド 最小",
  "version": "1.0.0",
  "stack": ["drizzle", "sqlite"],
  "tags": ["db", "json"],
  "description": "JSONライクなデータの保存（文字列化）最小例",
  "params": [ { "name": "app_name", "default": "drizzle-json" } ],
  "files": [
    { "path": "{{app_name}}/src/db/json.ts", "template": "import { drizzle } from 'drizzle-orm/better-sqlite3';\nimport Database from 'better-sqlite3';\nimport { sqliteTable, integer, text } from 'drizzle-orm/sqlite-core';\nconst sqlite = new Database('app.db');\nconst db = drizzle(sqlite);\nconst docs = sqliteTable('docs', { id: integer('id').primaryKey(), data: text('data').notNull() });\nexport async function save(obj: any){ return db.insert(docs).values({ data: JSON.stringify(obj) }); }\n" }
  ]
}

