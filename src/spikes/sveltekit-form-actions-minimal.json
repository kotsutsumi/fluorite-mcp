{
  "id": "sveltekit-form-actions-minimal",
  "name": "SvelteKit フォーム + actions 最小",
  "version": "1.0.0",
  "stack": ["sveltekit"],
  "tags": ["sveltekit", "form", "actions"],
  "description": "SvelteKitのFormと+page.server.tsのactionsでPOSTを処理する最小例。",
  "files": [
    { "path": "src/routes/+page.svelte", "template": "<script lang=\"ts\">export let form;</script>\n<h1>Form</h1>\n<form method=\"POST\">\n  <input name=\"name\" placeholder=\"name\"/>\n  <button>Send</button>\n</form>\n{#if form}<pre>{JSON.stringify(form)}</pre>{/if}\n" },
    { "path": "src/routes/+page.server.ts", "template": "import type { Actions } from './$types';\nexport const actions: Actions = { default: async ({ request })=>{ const fd = await request.formData(); return { ok: true, name: fd.get('name') }; } };\n" }
  ]
}

