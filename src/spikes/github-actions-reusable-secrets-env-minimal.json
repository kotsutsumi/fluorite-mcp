{
  "id": "github-actions-reusable-secrets-env-minimal",
  "name": "GH Actions 再利用WF + secrets/env 最小",
  "version": "1.0.0",
  "stack": ["github-actions", "yaml"],
  "tags": ["ci"],
  "description": "再利用WFとsecrets/envの併用例",
  "params": [ ],
  "files": [ { "path": ".github/workflows/reuse.yml", "template": "name: reuse\non: [workflow_call]\njobs: { build: { runs-on: ubuntu-latest, steps: [ { run: 'echo $SECRET' } ] } }\n" } ]
}

