{
  "id": "fastify-rate-limit-minimal",
  "name": "Fastify レート制限 最小",
  "version": "1.0.0",
  "stack": ["fastify"],
  "tags": ["web", "ratelimit"],
  "description": "@fastify/rate-limit を使う",
  "params": [ ],
  "files": [ { "path": "fastify/limit.js", "template": "const Fastify = require('fastify');\nconst rateLimit = require('@fastify/rate-limit');\nconst app = Fastify();\napp.register(rateLimit, { max: 10, timeWindow: '1 minute' });\napp.get('/', (_req, res)=> res.send('ok'));\napp.listen({ port: 3000 });\n" } ]
}

