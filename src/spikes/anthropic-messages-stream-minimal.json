{
  "id": "anthropic-messages-stream-minimal",
  "name": "Anthropic Messages Streaming (Node)",
  "version": "1.0.0",
  "stack": ["node", "anthropic"],
  "tags": ["llm", "stream"],
  "description": "Stream messages from Anthropic using @anthropic-ai/sdk.",
  "params": [
    { "name": "app_name", "default": "anthropic-stream-app" },
    { "name": "anthropic_model", "default": "claude-3-5-sonnet-20240620" }
  ],
  "files": [
    {
      "path": "{{app_name}}/index.mjs",
      "template": "import Anthropic from '@anthropic-ai/sdk';\nconst client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });\nconst stream = await client.messages.stream({\n  model: '{{anthropic_model}}',\n  max_tokens: 128,\n  messages: [{ role: 'user', content: 'Stream a short hello' }]\n});\nfor await (const ev of stream) {\n  if (ev.type === 'content_block_delta' && ev.delta?.type === 'text_delta') {\n    process.stdout.write(ev.delta.text || '');\n  }\n}\nconsole.log();\n"
    },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"@anthropic-ai/sdk\": \"latest\" } }\n" },
    { "path": "{{app_name}}/.env.example", "template": "ANTHROPIC_API_KEY=sk-ant-...\n" }
  ],
  "patches": []
}

