{
  "id": "sops-encrypt-file-minimal",
  "name": "sops 暗号化ファイル 最小",
  "version": "1.0.0",
  "stack": ["sops"],
  "tags": ["security"],
  "description": "sops で YAML を暗号化 (概念)",
  "params": [ ],
  "files": [
    { "path": "security/sops.txt", "template": "sops -e config.yaml > config.enc.yaml\n" }
  ]
}

