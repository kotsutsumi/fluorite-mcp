{
  "id": "langfuse-node-minimal",
  "name": "Langfuse Tracing Minimal (Node)",
  "version": "1.0.0",
  "stack": ["node", "observability", "langfuse"],
  "tags": ["observability", "tracing"],
  "description": "Initialize Langfuse client and create a minimal trace & generation.",
  "params": [
    { "name": "app_name", "default": "langfuse-minimal" }
  ],
  "files": [
    { "path": "{{app_name}}/index.mjs", "template": "import { Langfuse } from 'langfuse';\nconst lf = new Langfuse({ secretKey: process.env.LANGFUSE_SECRET_KEY, publicKey: process.env.LANGFUSE_PUBLIC_KEY, baseUrl: process.env.LANGFUSE_BASE_URL });\nconst trace = lf.trace({ name: 'demo-trace' });\ntrace.generation({ name: 'demo-gen', output: 'hello world' });\nawait lf.shutdownAsync();\nconsole.log('Langfuse trace sent');\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"langfuse\": \"latest\" } }\n" },
    { "path": "{{app_name}}/.env.example", "template": "LANGFUSE_PUBLIC_KEY=\nLANGFUSE_SECRET_KEY=\nLANGFUSE_BASE_URL=https://cloud.langfuse.com\n" }
  ],
  "patches": []
}

