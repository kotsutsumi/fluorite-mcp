{
  "id": "prisma-mongodb-minimal",
  "name": "Prisma + MongoDB Minimal",
  "version": "1.0.0",
  "stack": ["node", "prisma", "mongodb"],
  "tags": ["db"],
  "description": "Minimal Prisma schema and script for MongoDB.",
  "params": [
    { "name": "app_name", "default": "prisma-mongo" },
    { "name": "mongo_uri", "default": "mongodb://localhost:27017/prisma" }
  ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{\n  \"name\": \"{{app_name}}\",\n  \"private\": true,\n  \"scripts\": { \"postinstall\": \"prisma generate\", \"seed\": \"node script.js\" },\n  \"dependencies\": { \"@prisma/client\": \"latest\" },\n  \"devDependencies\": { \"prisma\": \"latest\" }\n}\n" },
    { "path": "{{app_name}}/prisma/schema.prisma", "template": "generator client { provider = \"prisma-client-js\" }\ndatasource db { provider = \"mongodb\" url = \"{{mongo_uri}}\" }\nmodel User { id String @id @default(auto()) @map(\"_id\") @db.ObjectId name String }\n" },
    { "path": "{{app_name}}/script.js", "template": "const { PrismaClient } = require('@prisma/client');\nconst prisma = new PrismaClient();\n(async () => {\n  await prisma.user.create({ data: { name: 'Alice' } });\n  const count = await prisma.user.count();\n  console.log('User count', count);\n  await prisma.$disconnect();\n})();\n" }
  ],
  "patches": []
}

