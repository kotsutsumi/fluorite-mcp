{
  "id": "hono-jwt-auth-middleware-minimal",
  "name": "Hono JWT 認証ミドルウェア 最小",
  "version": "1.0.0",
  "stack": ["hono", "jwt"],
  "tags": ["web", "auth"],
  "description": "Hono で JWT を検証してガード",
  "files": [
    { "path": "server.ts", "template": "import { Hono } from 'hono';\nconst app = new Hono();\napp.use('*', async (c, next)=>{ const auth = c.req.header('authorization')||''; if(!auth.startsWith('Bearer ')) return c.text('unauthorized', 401); return next(); });\napp.get('/', c=> c.text('ok'));\nexport default app;\n" }
  ]
}

