{
  "id": "argon2-password-hash-minimal",
  "name": "argon2 パスワードハッシュ",
  "version": "1.0.0",
  "stack": ["node", "argon2"],
  "tags": ["security"],
  "description": "argon2 でハッシュ/検証の最小例。",
  "params": [ { "name": "app_name", "default": "argon2-hash" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"scripts\": { \"start\": \"node run.js\" }, \"dependencies\": { \"argon2\": \"latest\" } }\n" },
    { "path": "{{app_name}}/run.js", "template": "const argon2 = require('argon2');\n(async () => { const hash = await argon2.hash('password'); console.log('hash', hash); console.log('verify', await argon2.verify(hash, 'password')); })();\n" }
  ],
  "patches": []
}

