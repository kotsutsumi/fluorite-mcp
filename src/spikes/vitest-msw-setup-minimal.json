{
  "id": "vitest-msw-setup-minimal",
  "name": "Vitest + MSW セットアップ 最小",
  "version": "1.0.0",
  "stack": ["vitest", "msw"],
  "tags": ["test"],
  "description": "Vitest で MSW を使う最小セットアップ",
  "params": [ ],
  "files": [
    { "path": "vitest.setup.ts", "template": "import { setupServer } from 'msw/node';\nconst server = setupServer();\nbeforeAll(()=> server.listen());\nafterEach(()=> server.resetHandlers());\nafterAll(()=> server.close());\n" }
  ]
}

