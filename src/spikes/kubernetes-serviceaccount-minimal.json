{
  "id": "kubernetes-serviceaccount-minimal",
  "name": "Kubernetes ServiceAccount 最小",
  "version": "1.0.0",
  "stack": ["kubernetes", "yaml"],
  "tags": ["k8s"],
  "description": "ServiceAccount の最小",
  "params": [ { "name": "name", "default": "default" } ],
  "files": [ { "path": "k8s/serviceaccount.yaml", "template": "apiVersion: v1\nkind: ServiceAccount\nmetadata: { name: {{name}} }\n" } ]
}

