{
  "id": "square-payments-create-minimal",
  "name": "Square Payments Create 最小",
  "version": "1.0.0",
  "stack": ["node", "square"],
  "tags": ["payments"],
  "description": "Square API で支払い作成",
  "params": [ { "name": "app_name", "default": "square-pay" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{\\n  \"name\": \"{{app_name}}\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": { \"start\": \"node run.js\" },\n  \"dependencies\": { \"square\": \"^38\" }\n}\n" },
    { "path": "{{app_name}}/run.js", "template": "import { Client } from 'square';\nconst client = new Client({ accessToken: process.env.SQUARE_ACCESS_TOKEN, environment: 'sandbox' });\nconsole.log('Use client.paymentsApi.createPayment(...)');\n" }
  ]
}

