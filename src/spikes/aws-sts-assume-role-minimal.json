{
  "id": "aws-sts-assume-role-minimal",
  "name": "AWS STS AssumeRole 最小",
  "version": "1.0.0",
  "stack": ["aws", "sts"],
  "tags": ["security"],
  "description": "AssumeRole を使用して一時クレデンシャル取得",
  "params": [ { "name": "roleArn", "default": "arn:aws:iam::123:role/app" } ],
  "files": [ { "path": "aws/sts-assume.mjs", "template": "import { STSClient, AssumeRoleCommand } from '@aws-sdk/client-sts';\nconst c = new STSClient({});\nconsole.log(await c.send(new AssumeRoleCommand({ RoleArn: process.env.ROLE_ARN||'{{roleArn}}', RoleSessionName: 'session' })));\n" } ]
}

