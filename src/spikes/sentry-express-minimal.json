{
  "id": "sentry-express-minimal",
  "name": "Sentry + Express",
  "version": "1.0.0",
  "stack": ["node", "express", "sentry"],
  "tags": ["error-tracking"],
  "description": "Sentry SDK を Express に組込み、エラーを送信。",
  "params": [ { "name": "app_name", "default": "sentry-express" }, { "name": "port", "default": "3000" } ],
  "files": [
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"private\": true, \"scripts\": { \"start\": \"node index.js\" }, \"dependencies\": { \"express\": \"latest\", \"@sentry/node\": \"latest\" } }\n" },
    { "path": "{{app_name}}/index.js", "template": "const express = require('express');\nconst Sentry = require('@sentry/node');\nSentry.init({ dsn: process.env.SENTRY_DSN||'' });\nconst app = express();\napp.use(Sentry.Handlers.requestHandler());\napp.get('/error', ()=> { throw new Error('boom'); });\napp.use(Sentry.Handlers.errorHandler());\napp.get('/health', (_req,res)=>res.json({ status:'ok' }));\napp.listen(Number('{{port}}'), ()=> console.log('listening on {{port}}'));\n" },
    { "path": "{{app_name}}/README.md", "template": "SENTRY_DSN を設定してください。\n" }
  ],
  "patches": []
}

