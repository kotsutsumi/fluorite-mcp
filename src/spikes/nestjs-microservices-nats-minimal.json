{
  "id": "nestjs-microservices-nats-minimal",
  "name": "NestJS Microservices NATS 最小",
  "version": "1.0.0",
  "stack": ["nestjs", "nats"],
  "tags": ["microservices"],
  "description": "NestJS の NATS マイクロサービス起動スニペット",
  "params": [ ],
  "files": [
    { "path": "nest/main.ts", "template": "import { NestFactory } from '@nestjs/core';\nimport { MicroserviceOptions, Transport } from '@nestjs/microservices';\nimport { AppModule } from './app.module';\n(async () => {\n  const app = await NestFactory.createMicroservice<MicroserviceOptions>(AppModule, { transport: Transport.NATS });\n  await app.listen();\n})();\n" }
  ]
}

