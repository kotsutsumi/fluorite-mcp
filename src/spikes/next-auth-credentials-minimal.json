{
  "id": "next-auth-credentials-minimal",
  "name": "NextAuth Credentials 最小",
  "version": "1.0.0",
  "stack": ["nextjs", "next-auth"],
  "tags": ["auth"],
  "description": "Credentials プロバイダでの NextAuth 設定最小例",
  "params": [ { "name": "app_name", "default": "next-app" } ],
  "files": [
    { "path": "{{app_name}}/pages/api/auth/[...nextauth].ts", "template": "import NextAuth from 'next-auth';\nimport CredentialsProvider from 'next-auth/providers/credentials';\nexport default NextAuth({\n  providers: [CredentialsProvider({\n    name: 'Credentials',\n    credentials: { username: {}, password: {} },\n    async authorize(creds) {\n      if (creds?.username === 'user' && creds?.password === 'pass') return { id: '1', name: 'User' };\n      return null;\n    }\n  })]\n});\n" }
  ]
}

