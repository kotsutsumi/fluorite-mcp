{
  "id": "prisma-relations",
  "name": "Prisma Relations (User-Post)",
  "version": "1.0.0",
  "stack": ["prisma", "postgres"],
  "tags": ["db", "relations"],
  "description": "User-Post 1:N relation schema and sample create.",
  "params": [{ "name": "app_name", "default": "prisma-app" }],
  "files": [
    { "path": "{{app_name}}/prisma/schema.prisma", "template": "generator client { provider = \"prisma-client-js\" }\ndatasource db { provider = \"postgresql\" url = env(\"DATABASE_URL\") }\nmodel User { id Int @id @default(autoincrement()) email String @unique posts Post[] }\nmodel Post { id Int @id @default(autoincrement()) title String author User @relation(fields: [authorId], references:[id]) authorId Int }\n" },
    { "path": "{{app_name}}/seed.mjs", "template": "import { PrismaClient } from '@prisma/client';\nconst prisma = new PrismaClient();\nawait prisma.user.create({ data: { email: 'a@example.com', posts: { create: [{ title: 'Hello' }] } } });\nconsole.log('seeded');\nawait prisma.$disconnect();\n" },
    { "path": "{{app_name}}/.env.example", "template": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres\n" }
  ],
  "patches": []
}

