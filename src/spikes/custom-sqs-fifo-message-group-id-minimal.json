{
  "id": "custom-sqs-fifo-message-group-id-minimal",
  "name": "SQS FIFO MessageGroupId 最小",
  "version": "1.0.0",
  "stack": ["sqs"],
  "tags": ["messaging", "aws"],
  "description": "MessageGroupId利用の概念スタブ",
  "files": [
    { "path": "src/aws/sqs.fifo.group.ts", "template": "export async function send(groupId:string, body:string){ return { groupId, body }; }\n" }
  ]
}

