{
  "id": "chromadb-where-document-not-contains-minimal",
  "name": "ChromaDB where_document Not Contains Minimal (Node)",
  "version": "1.0.0",
  "stack": ["node", "chromadb"],
  "tags": ["filter"],
  "description": "Use where_document with $notContains to exclude terms.",
  "params": [{ "name": "app_name", "default": "chromadb-where-not" }],
  "files": [
    { "path": "{{app_name}}/index.mjs", "template": "import { ChromaClient } from 'chromadb';\nconst client = new ChromaClient();\nconst col = await client.getOrCreateCollection({ name: 'docs' });\nawait col.add({ ids: ['1','2'], documents: ['hello world', 'fluorite mcp'] });\nconst res = await col.query({ queryTexts: ['hello'], nResults: 2, where_document: { $notContains: 'fluorite' } });\nconsole.log(res);\n" },
    { "path": "{{app_name}}/package.json", "template": "{ \"name\": \"{{app_name}}\", \"type\": \"module\", \"private\": true, \"dependencies\": { \"chromadb\": \"latest\" } }\n" }
  ],
  "patches": []
}

