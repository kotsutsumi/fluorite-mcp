{
  "id": "knex-querybuilder-minimal",
  "name": "Knex QueryBuilder 最小",
  "version": "1.0.0",
  "stack": ["knex", "node"],
  "tags": ["sql"],
  "description": "Knex での単純クエリ",
  "params": [ ],
  "files": [
    { "path": "knex/index.js", "template": "const knex = require('knex')({ client: 'pg', connection: process.env.DATABASE_URL });\n(async()=>{ console.log(await knex.raw('select 1')); await knex.destroy(); })();\n" }
  ]
}

