{
  "id": "nextauth-credentials-provider-minimal",
  "name": "NextAuth Credentials Provider 最小",
  "version": "1.0.0",
  "stack": ["nextauth"],
  "tags": ["auth"],
  "description": "資格情報プロバイダの最小例",
  "files": [
    { "path": "pages/api/auth/[...nextauth].ts", "template": "import NextAuth from 'next-auth';\nimport Credentials from 'next-auth/providers/credentials';\nexport default NextAuth({ providers: [ Credentials({ name: 'Credentials', credentials: { email: {}, password: {} }, async authorize(c){ if(c?.email==='a'&&c.password==='b') return { id:'1', name:'A' }; return null; } }) ] });\n" }
  ]
}

