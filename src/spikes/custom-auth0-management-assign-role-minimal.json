{
  "id": "custom-auth0-management-assign-role-minimal",
  "name": "Auth0 Management ロール付与 最小",
  "version": "1.0.0",
  "stack": ["auth0"],
  "tags": ["auth"],
  "description": "Auth0管理APIでユーザーにロール付与（スタブ）",
  "files": [
    { "path": "src/auth/auth0.mgmt.ts", "template": "export async function assignRole(userId:string, roleId:string){ return { userId, roleId }; }\n" }
  ]
}

