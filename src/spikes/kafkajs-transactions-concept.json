{
  "id": "kafkajs-transactions-concept",
  "name": "KafkaJS トランザクション（概念）",
  "version": "1.0.0",
  "stack": ["node", "kafkajs"],
  "tags": ["messaging"],
  "description": "KafkaJS のトランザクション利用はブローカー設定が必要（概念）。",
  "params": [ { "name": "app_name", "default": "kafkajs-tx" } ],
  "files": [
    { "path": "{{app_name}}/README.md", "template": "Kafka トランザクションは idempotent producer 等の設定が必要です。本テンプレは概念説明です。\n" }
  ],
  "patches": []
}

