{
  "id": "aws-sns-sms-publish-minimal",
  "name": "AWS SNS SMS Publish 最小",
  "version": "1.0.0",
  "stack": ["aws", "sns"],
  "tags": ["notify"],
  "description": "SNS で SMS 送信",
  "params": [ ],
  "files": [ { "path": "aws/sns-sms.mjs", "template": "import { SNSClient, PublishCommand } from '@aws-sdk/client-sns';\nconst c = new SNSClient({});\nawait c.send(new PublishCommand({ PhoneNumber: process.env.PHONE, Message: 'hello' }));\nconsole.log('ok');\n" } ]
}

