{
  "id": "drizzle-transaction-minimal",
  "name": "Drizzle Transaction 最小",
  "version": "1.0.0",
  "stack": ["drizzle", "sqlite"],
  "tags": ["db", "transaction"],
  "description": "Drizzle のトランザクション最小例（概念）",
  "params": [ { "name": "app_name", "default": "drizzle-tx" } ],
  "files": [
    { "path": "{{app_name}}/src/db/tx.ts", "template": "import { drizzle } from 'drizzle-orm/better-sqlite3';\nimport Database from 'better-sqlite3';\nconst sqlite = new Database('app.db');\nconst db = drizzle(sqlite);\nexport async function run(){ await db.transaction(async (tx)=>{ /* tx.insert(...) */ }); }\n" }
  ]
}

