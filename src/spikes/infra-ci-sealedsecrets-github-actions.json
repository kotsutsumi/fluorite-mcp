{
  "id": "infra-ci-sealedsecrets-github-actions",
  "name": "Infra CI SealedSecrets (GitHub Actions) 最小",
  "version": "1.0.0",
  "stack": ["kubernetes", "ci", "sealedsecrets"],
  "tags": ["infra", "ci", "sealedsecrets"],
  "description": "SealedSecretsをCIで適用する最小例（復号不要、Controllerに復号を委任）。",
  "files": [
    { "path": ".github/workflows/sealedsecrets.yml", "template": "name: Apply SealedSecrets\n\non:\n  workflow_dispatch: {}\n\njobs:\n  apply:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Apply SealedSecret\n        run: |\n          kubectl apply -f k8s/sealedsecret.yaml\n" }
  ]
}

