{
  "id": "custom-nginx-reverse-proxy-config-minimal",
  "name": "NGINX リバースプロキシ設定 最小",
  "version": "1.0.0",
  "stack": ["nginx"],
  "tags": ["proxy"],
  "description": "NGINXの最小リバプロ設定",
  "files": [
    { "path": "nginx/nginx.conf", "template": "events{} http{ server{ listen 80; location / { proxy_pass http://app; } } }\n" }
  ]
}

